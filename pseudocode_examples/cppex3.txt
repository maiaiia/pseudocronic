#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double a, s;
    int b, d, e, f;

    if (((1 != 1) && true)) {
        cout << "B";
    }
    do {
        a = sqrt(pow(2, 2));
    } while (((a == 6) && (a == 3)));
    a = 10;
    b = 20;
    cin >> d >> e >> f;
    while ((a == 3)) {
        s = (a + (b / 3));
    }
    return 0;
}